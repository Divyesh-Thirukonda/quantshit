# API Keys (required for live trading)
POLYMARKET_API_KEY=your_polymarket_api_key_here
KALSHI_API_KEY=your_kalshi_api_key_here

# Note: Trading parameters (min_volume, min_spread, etc.) are now
# configured per-strategy in code, not via environment variables.
# See src/strategies/config.py for strategy configurations.